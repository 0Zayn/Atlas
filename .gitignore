# Build output (regenerated by the build)
Build/
Intermediates/

# Visual Studio
.vs/
*.user
*.suo

# Runtime files
atlas.layout

# Note: Package/ is the prebuilt SDK (headers + Atlas.lib) and is intentionally tracked.
